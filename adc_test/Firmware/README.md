# Firmware code for ADC test


Need all c files lists here, main.c is the main firmware program and xusbps_* files are for enumerating the device as a generic USB peripheral. ps7_init.c and ps7_init.h are needed to initialize the PS for the Eclypse Z7, but if you are using another board, you need to regenerate these files in AMD Vitis. 